# Demo React App

Goal:
- Simple chat box that calls `evaluateMessage()`
- Shows inline hints (nudges + MQS) above the input

MVP UI:
- Message input
- "Send" button
- Inline hint bar (top): tone/clarity message + suggested rewrite
