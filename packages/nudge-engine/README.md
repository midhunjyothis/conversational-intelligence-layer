# Nudge Engine

Role:
- Load nudges from Toon (`nudges.catalog.v0.toon`)
- Match incoming `triggers` from detectors
- Select 0–3 best nudges per message
- Provide `before/after` examples + micro_copy

Future:
- Add weighting by MQS sub-scores
- Add A/B buckets for different nudge styles
