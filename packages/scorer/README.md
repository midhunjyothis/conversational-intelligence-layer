# Scorer

Role:
- Convert detected signals → MQS (0–100)
- Compute sub-scores: clarity, respect, empathy, specificity, actionability
- Apply weights + penalties from Toon specs (future)

Output:
- `mqs`
- `sub_scores`
- optional `rationale[]`
