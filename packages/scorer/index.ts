// MQS scorer entrypoint (placeholder)

export function scoreMessage(_triggers: string[]): any {
  // TODO: compute MQS + sub-scores from triggers/signals
  return {
    mqs: 0,
    sub_scores: {},
  };
}
